[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "conversations-mcp"
version = "0.1.0"
dependencies = [
    "mcp",
]

[project.scripts]
conversations = "conversations.server:main"
